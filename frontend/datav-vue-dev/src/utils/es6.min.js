Array.prototype.isNull=function(){return!!Array.isArray(this)&&0==this.length},Array.prototype.equals=function(a){// if the other array is a falsy value, return
if(!a)return!1;// compare lengths - can save a lot of time
if(this.length!=a.length)return!1;for(var b=0,c=this.length;b<c;b++)// Check if we have nested arrays
if(this[b]instanceof Array&&a[b]instanceof Array){// recurse into the nested arrays
if(!this[b].equals(a[b]))return!1;}else if(this[b]!=a[b])// Warning - two different object instances will never be equal: {x:20} != {x:20}
return!1;return!0};