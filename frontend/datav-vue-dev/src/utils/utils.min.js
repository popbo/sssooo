import { validatenull } from "@/echart/util";
export const uuid = () => {
  for (var a = [], b = "0123456789abcdef", c = 0; 36 > c; c++)
    a[c] = b.substr(Math.floor(16 * Math.random()), 1);
  (a[14] = "4"),
    (a[19] = b.substr(8 | (3 & a[19]), 1)),
    (a[8] = a[13] = a[18] = a[23] = "-");
  var d = a.join("");
  return d;
};
export const createFile = () => ({
  title: "\u6587\u4EF6\u5939",
  name: "\u6587\u4EF6\u5939",
  index: uuid(),
  children: [],
});
export const compare = (a) =>
  function (b, c) {
    var d = b[a],
      e = c[a];
    return e < d ? -1 : e > d ? 1 : 0;
  };
export const funEval = (a) => new Function("return " + a + ";")();
export const getFunction = (a, b) => {
  if (!validatenull(a))
    try {
      return funEval(a);
    } catch {
      return () => {};
    }
  else if (b) return () => {};
};
export const getJson = (a) => {
  if (validatenull(a)) return {};
  if ("string" == typeof a)
    try {
      return JSON.parse(a);
    } catch {
      return {};
    }
  else return a;
};
export const checkUrl = (a) => {
  return !!/http(s)?:\/\/([\w-.]+)+(:[0-9]+)?.*$/.test(a);
};
